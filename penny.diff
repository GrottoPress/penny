diff --git a/.github/workflows/ci.yml b/.github/workflows/ci.yml
index baab970..235c156 100644
--- a/.github/workflows/ci.yml
+++ b/.github/workflows/ci.yml
@@ -21,7 +21,7 @@ jobs:
     continue-on-error: false
     steps:
       - name: Download source
-        uses: actions/checkout@v2
+        uses: actions/checkout@v3
       - name: Install Crystal
         uses: crystal-lang/install-crystal@v1
         with:
@@ -43,13 +43,13 @@ jobs:
     continue-on-error: ${{ matrix.experimental }}
     steps:
       - name: Download source
-        uses: actions/checkout@v2
+        uses: actions/checkout@v3
       - name: Install Crystal
         uses: crystal-lang/install-crystal@v1
         with:
           crystal: ${{ matrix.crystal }}
       - name: Cache shards
-        uses: actions/cache@v2
+        uses: actions/cache@v3
         with:
           path: ~/.cache/shards
           key: ${{ runner.os }}-shards-${{ hashFiles('shard.yml') }}
@@ -75,7 +75,6 @@ jobs:
           EMAIL_REPLY_TO: support@my-app.tld
           GZIP_ENABLED: "false"
           DATABASE_HOST: localhost
-          DATABASE_NAME: app_development
           DATABASE_PASSWORD: password
           DATABASE_PORT: "5432"
           DATABASE_USERNAME: postgres
diff --git a/shard.yml b/shard.yml
index 4d04af7..bb4ce27 100644
--- a/shard.yml
+++ b/shard.yml
@@ -30,13 +30,13 @@ dependencies:
     version: ~> 0.3.0
   envy:
     github: GrottoPress/envy
-    version: ~> 0.3.0
+    version: ~> 0.4.0
   i18n:
     github: crystal-i18n/i18n
     version: ~> 0.2.1
   shield:
     github: GrottoPress/shield
-    version: ~> 0.18.0
+    version: ~> 0.19.0
   unslash:
     github: GrottoPress/unslash
     version: ~> 0.1.0
diff --git a/spec/spec_helper.cr b/spec/spec_helper.cr
index dd4ba7b..710a0da 100644
--- a/spec/spec_helper.cr
+++ b/spec/spec_helper.cr
@@ -1,4 +1,5 @@
 ENV["LUCKY_ENV"] = "test"
+ENV["DATABASE_NAME"] = "penny_test"
 
 require "spec"
 require "lucky_flow"
diff --git a/src/actions/private_api.cr b/src/actions/private_api.cr
index a014e2a..5d224e8 100644
--- a/src/actions/private_api.cr
+++ b/src/actions/private_api.cr
@@ -11,7 +11,6 @@ abstract class PrivateApi < Lucky::Action
   include Shield::ApiAction
 
   include Shield::Api::LoginHelpers
-  include Shield::Api::BearerLoginHelpers
   include Shield::Api::EmailConfirmationHelpers
   include Shield::Api::PasswordResetHelpers
 
diff --git a/src/actions/public_api.cr b/src/actions/public_api.cr
index adf47ed..6b2f78b 100644
--- a/src/actions/public_api.cr
+++ b/src/actions/public_api.cr
@@ -9,20 +9,11 @@
 abstract class PublicApi < Lucky::Action
   include Shield::ApiAction
 
-  include Shield::Api::LoginHelpers
   include Shield::Api::BearerLoginHelpers
   include Shield::Api::EmailConfirmationHelpers
   include Shield::Api::PasswordResetHelpers
 
-  include Shield::Api::LoginPipes
-
-  # IMPORTANT!:
-  #   This module reduces `Shield::Api::LoginPipes` from an authentication
-  #   pipe to a delegated authorization pipe. Because of this, bearer tokens
-  #   retrieved from the `Authorization` header MUST NOT be used as proof
-  #   of authentication.
   include Shield::Api::BearerLoginPipes
-
   include Shield::Api::EmailConfirmationPipes
   include Shield::Api::PasswordResetPipes
 
